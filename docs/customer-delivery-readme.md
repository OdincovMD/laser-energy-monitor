# Laser Energy Monitor: памятка к поставке

## Что входит в поставку

В поставку входит папка с `Release`-сборкой приложения `Laser Energy Monitor` для `.NET Framework 4.8`.

Основные файлы:

- `LaserEnergyMonitor.Wpf.exe`
- `LaserEnergyMonitor.Wpf.exe.config`
- библиотеки `LaserEnergyMonitor.*.dll`
- библиотеки `DocumentFormat.OpenXml*.dll`
- эта памятка
- инструкция по первому запуску проверки `Ophir`

## Что нужно для запуска

Минимально:

- Windows с установленным `.NET Framework 4.8`
- права на чтение и запись в папку, где лежит приложение

Для работы только в режиме имитации:

- дополнительных vendor runtime не требуется

Для работы с реальным `BeamGage`:

- установлен `BeamGage Professional` с automation support
- доступны vendor assembly:
  `Spiricon.Automation.dll` и `Spiricon.BeamGage.Automation.dll`
- установленный runtime совместим с `x86`, так как приложение собрано под `x86`

Для работы с реальным `Ophir`:

- установлен vendor automation package / COM runtime
- в системе зарегистрирован `OphirLMMeasurement.CoLMMeasurement`
- установленный runtime совместим с `x86`
- устройство видно по USB и не занято другой программой

## Что не требуется

- Microsoft Excel для формирования `.xlsx` не требуется
- отдельная база данных не требуется
- отдельная серверная часть не требуется

## Как запускать

1. Распаковать папку поставки в локальный каталог, где у пользователя есть право записи.
2. Запустить `LaserEnergyMonitor.Wpf.exe`.
3. При первом запуске проверить выбранные источники данных в блоке `Session Control`.

По умолчанию приложение может работать в безопасном режиме с симулированными источниками:

- `Simulated BeamGage`
- `Simulated Ophir`

Это удобно для первичной проверки интерфейса и экспорта без подключения приборов.

## Что приложение создаёт рядом с собой

При запуске приложение создаёт подпапку `output`.

В ней могут появляться:

- `application.log`
- `measurement-session.xlsx`
- `measurement-session.RawData.csv`
- `measurement-session.Events.csv`
- `measurement-session.Summary.csv`
- `measurement-session.Stationary.csv`
- `hardware-self-test-*.txt`
- `ophir-smoke-test-*.txt`
- подпапка `ophir-captures` при включённом захвате сырых данных `Ophir`

Важно:

- для записи результатов у пользователя должен быть доступ на запись в каталог поставки или в выбранный им путь экспорта
- если в поле `Output Path` указать свой путь, приложение будет писать итоговый `.xlsx` и shadow CSV туда

## Что уже проверено в текущей поставке

На стороне разработки подтверждено:

- `Debug` и `Release` сборки проходят без ошибок
- юнит-тесты проходят: `8/8`
- UI smoke-тест проходит: `13/13`
- экспорт `.xlsx` формируется через `Open XML`
- доступны диагностические сценарии:
  `Run Self-Test`, `BeamGage Smoke-Test`, `Ophir Smoke-Test`

## Что рекомендуется проверить у заказчика

Перед финальной приёмкой рекомендуется выполнить:

1. Запуск приложения на целевой машине.
2. Проверку `Run Self-Test`.
3. Проверку `BeamGage Smoke-Test`, если используется реальный `BeamGage`.
4. Проверку `Ophir Smoke-Test`, если используется реальный `Ophir`.
5. Короткую реальную сессию измерения с сохранением результатов в `xlsx`.

## Если планируется работа с реальным Ophir

Используйте отдельную инструкцию:

- `customer-ophir-first-run.md`

В ней описано:

- что должно быть подготовлено заранее
- как выполнить первую диагностику
- какие файлы нужно вернуть после проверки

## Если планируется работа с реальным BeamGage

Перед первым запуском на машине заказчика нужно убедиться, что:

- `BeamGage Professional` установлен локально
- automation components установлены вместе с основной программой
- приложение запускается обычным интерактивным пользователем Windows

Если зависимость не найдена, приложение покажет диагностическую ошибку вместо необработанного сбоя.

## Ограничения текущей поставки

- приложение собрано под `x86`, поэтому vendor runtime тоже должен быть совместим по разрядности
- успешная локальная сборка и тесты не заменяют проверку на конкретном стенде заказчика
- итоговая готовность к промышленной эксплуатации зависит от реальной проверки `BeamGage` и `Ophir` на целевой машине
